Perform exact string replacements in a file. file_path accepts absolute paths, relative paths, and @bundle-name:path bundle resources (full form in read_file's description).

Hard constraints:
- You must have called read_file on the file at least once in this conversation; editing without reading errors.
- The edit will FAIL if `old_string` is not unique in the file. Provide a larger old_string with more surrounding context, or use `replace_all` to change every instance (also how to rename a variable across a file).
- new_string must differ from old_string.
- When editing text from read_file output, preserve the exact indentation (tabs/spaces) as it appears AFTER the line number prefix (prefix = spaces + line number + tab). Never include any part of that prefix in old_string or new_string.

ALWAYS prefer editing an existing file; NEVER write new files unless explicitly required. Do not add emojis unless asked.