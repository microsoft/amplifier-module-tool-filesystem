# DONE-NOTE — 3ahq-tooldesc-filesystem

Item `model_performance-3ahq` (this repo's slice) · lane `3ahq-tooldesc-filesystem` · 2026-09-07
Repo `microsoft/amplifier-module-tool-filesystem` · branch `lane/3ahq-tooldesc-filesystem`
Stock ref (merge-base with `origin/main`): `aeadf4e3d1e2308415166ebccdad7fda17d6db04`

## TERMINAL OUTCOME: **A — RESOLVED**

Chosen once. Every reachable deliverable is DONE and shipped as a draft PR; the
two unreachable ones are unreachable **because the goal named tools that are not
in this repository** (F1), which GOAL.md itself classifies as a defect to report
and resolve past — not to block on, and not a cap effect. **The cap never bound:
authority $0, spend $0.00.**

Per the landing-stage clause: the merge is the manager's next stage. Everything
below is demonstrated fail-before/pass-after and shipped for landing.

---

## The result in one table

| tool | stock | shipped | saved | status |
|---|---:|---:|---:|---|
| `read_file` | 1,074 | 725 | **349** | applied, v1 text verbatim |
| `write_file` | 867 | 567 | **300** | applied, v1 text verbatim |
| `edit_file` | 1,345 | 923 | **422** | applied **+ fidelity restoration (+36)** |
| **repo total** | **3,286** | **2,215** | **1,071** | |
| `grep` | 1,365 | — | — | **NOT-POSSIBLE — not in this repo** (F1) |
| `glob` | 904 | — | — | **NOT-POSSIBLE — not in this repo** (F1) |

Generated by `tools/fidelity_recheck.py` at today's head; raw output in
`evidence/fidelity-recheck-at-head.{txt,json}`. Stock is read from the merge-base
with `git show`, not from zc6t's table.

---

## Deliverables

### 1. All five patches applied — **DONE (3 of 5) · NOT-POSSIBLE (2 of 5)**

**Three applied, none force-applied, no fuzz used, none needed.** All three
before-sides verified **byte-identical** to the merge-base text before anything
was written (`evidence/patch-before-side-verification.txt`). The two commits that
landed after zc6t cut these patches — `51aaf2d` (line-ending preservation) and
`aeadf4e` (`file_path` dedup) — did **not** move the description text. Nothing
diverged; no hand-port was required.

`patch(1)` was **not** used, and could not have been, for two reasons that are
properties of the artifacts rather than divergences at head (F4):
the diffs target pseudo-files (`a/read_file.description`) that exist in no repo,
and four of five concatenate the final removed line with the first added line
because they were generated with no trailing newline and no
`\ No newline at end of file` marker. `tools/verify_patch_before_side.py`
reconstructs both sides with a **deterministic** split — the suffix after `+` must
be exactly a line of the authoritative `<tool>.lean.txt`, or nothing is split —
and asserts equality byte for byte.

**NOT-POSSIBLE, and the reason leads with what was executed:** `grep` and `glob`
were fetched, reconstructed and verified (`after == lean.txt` byte-for-byte for
both; `glob`'s before-side also verified against the installed package at 904/904)
— and then **not applied, because neither tool exists in this repository.** They
live in `amplifier-module-tool-search`. This is a defect in the goal, inherited
from a hard-coded wrong repo in zc6t's own generator and copied forward twice
unchecked; full provenance in FINDINGS F1. Per GOAL.md's own clause the patches
ship as artifacts under `upstream-patches/` and nothing outside this worktree was
touched. **676 chars (356 + 320) remain unrealised and need one sibling lane on
`amplifier-module-tool-search`.**

### 2. Fidelity table re-verified at today's head — **DONE**

Re-derived, not inherited. `tools/fidelity_recheck.py` extracts every
rule-shaped token from the merge-base text — backticked spans, `@mention`s, URLs,
ALL-CAPS imperatives, command-shaped lines, **plus numeric limits and
input-schema parameter names, which are this lane's additions to zc6t's
vocabulary** — and asserts each still appears in the shipped text.

**Result: 1 flag, reviewed, false positive. Expected none of substance; got none.**
The flag (`content` on `edit_file`) was raised by this lane's own added
vocabulary and is over-broad — `content` is not an `edit_file` parameter, and the
stock occurrence is prose restating the line-number-prefix rule, both halves of
which survive. The verdict is recorded in the script's `REVIEWED_FLAGS`; **the
script exits non-zero on any flag with no recorded human verdict**, so a future
weakening cannot pass silently.

**The `edit_file` restoration, carried forward and pinned.** zc6t found exactly
one real weakening across all 23 targets: v1 softened stock's *"ALWAYS prefer
editing existing files in the codebase. NEVER write new files unless explicitly
required."* into *"Prefer editing existing files over creating new ones."*,
dropping the imperative force and the qualifier. Restored as:

> ALWAYS prefer editing an existing file; NEVER write new files unless explicitly required.

byte-identical to the clause v1 **already uses in `write_file`** and did not flag,
so the two descriptions now state the same rule the same way.

**DECISION, recorded because the goal's number is wrong (F2): the restoration is
+36 chars, not +450.** GOAL.md says "restored in-repo at +450 chars" three times.
zc6t's FINDINGS F4 says the opposite in two adjacent paragraphs: `edit_file`'s
recommendation is *"restore the qualifier at a cost of **~35 chars**"*, and the
**+450 belongs to span 9** — a *context file* in *foundation*
(`anchors/context/system.md` + `anchors-amp-dev/context/amplifier-ecosystem.md`),
already shipped in PR #372 and pinned there by `TestFidelityBeatsCompression`.
Two targets, two repos, two numbers; the goal merged them. Taken literally the
instruction would have spent +450 against `edit_file`'s entire 458-char saving and
left this lane reporting an 8-char win. Followed zc6t's actual figure: **+36
(887 → 923), saving 458 → 422.** No human was waited on; the choice is recorded
here per SCOPE-OUTS.

### 3. Stock → lean char counts per tool, and the repo total — **DONE**

The table above. Repo total **3,286 → 2,215 = −1,071 chars**, re-measured at head,
not quoted from `fidelity-report.json` (though the three in-repo stock figures do
match it exactly: 1,074 / 867 / 1,345).

**Honest accounting against the framing:** the five-tool framing implies 1,783
chars for this lane. **1,071 realised, 676 unreachable from here** (F1).

### 4. Byte-for-byte pin test per description — **DONE**

`tests/test_tool_description_pin.py`, 8 tests:

- byte-for-byte equality against `tests/pins/<tool>.description.txt`, **plus**
  sha256 and char-count assertions per tool (a pin that must be recomputed
  deliberately, not just re-copied);
- `read_file` / `write_file` asserted **equal to zc6t's v1 lean text**, single-sourced
  at `tests/pins/v1/<tool>.lean.txt`, copied verbatim from foundation main;
- `edit_file` asserted to differ from v1 in **exactly** the one restored clause and
  nowhere else (`v1.replace(WEAK, RESTORED) == shipped`, and `len` delta == 36) —
  this repo's `TestFidelityBeatsCompression`, pinning the deviation *as* a
  deviation so it can be neither lost nor quietly widened;
- a cheap shape ratchet: no description may regrow a `Usage:` bullet block.

`tests/pins/.gitattributes` carries `* -text` so no EOL normalisation can perturb
a byte pin.

**The pin is demonstrated, not asserted** (`evidence/ratchet-demo.txt`): removing
the restoration turns the suite **RED** (2 failures) *and* makes the independent
fidelity re-check fire, **reproducing zc6t's exact `ALWAYS` flag**; reverting
returns both to green. Two independent instruments, same answer, on a known
input.

### 5. CI — **DONE (stated plainly): this repo has NO CI**

```
$ ls -la .github/workflows
ls: cannot access '.github/workflows': No such file or directory
```

There is no green CI run and this PR does not imply one. Evidence is the local
suite — `evidence/test-suite.txt`, **30 passed** (22 pre-existing, 8 new) — and
the ratchet demonstration. This repo is one of the 18 in `j1e6`; that CI lane is
sequenced **after** this one deliberately, so CI executes a suite containing the
pin rather than one that predates it.

### 6. Draft PR — **DONE.** See `publication` in `DONE.json` (read back from the remote, not from local `git log`). **Not merged; the manager merges.**

### 7. DONE-NOTE at the lane artifact root — **DONE.** This file, at
`docs/lanes/3ahq-tooldesc-filesystem/DONE-NOTE.md`. The repo-root `DONE-NOTE.md`
was **not** created or modified (item kez).

---

## Spend

**$0.00 against an authority of $0.00.** Arithmetic as stated in GOAL.md:
`0 runs × 0 arms × $0 / 1.00 = $0.00`, slack $0.00.

**The authoring-rule check was performed on first read and the arithmetic closes.**
No deliverable in this lane requires a purchase: applying measured patches,
re-deriving a fidelity table offline, and running a local suite are all `$0`.
There is no residue to report because there was never a budget to hold, and no
smallest-useful-purchase that a residue failed to buy. **No API call, no model
call, no DTU, no measurement was made or needed** — `g7h3` bought the $/task
answer at $428.10 and it was not re-bought.

**Infrastructure: none created.** Nothing registered in the infra ledger, nothing
claimed, nothing to tear down. `infra_ledger.sh … sweep` was **not** run (it is
the manager's batch-close verb and other lanes are live).

---

## Deviations, and what a reviewer should check first

1. **Two of five tools are not in this repo** (F1). The single highest-value
   follow-up is a sibling lane on `amplifier-module-tool-search` for the
   remaining 676 chars — and it should run **before** that repo's `j1e6-ci-*`
   lane, for the same reason this one ran before `j1e6-ci-tool-filesystem`.
2. **The goal's "+450 chars" is wrong** (F2). Shipped +36 per zc6t's actual
   finding. If a reviewer expects a ~450-char restoration in `edit_file`, they
   are reading the goal's transcription error, not this branch.
3. **`fidelity-report.json` misattributes `grep`/`glob`** to this repo, and its
   `grep` row's `stock_chars: 1365` does not match its own patch's before-side
   (1,364 — a dropped trailing newline, F5). Both are advisories for the
   `tool-search` lane, not defects in this branch.
4. **"Stash-compare default-mode byte-identity" does not apply here and is not
   claimed.** This change *is* a text change to three tool descriptions by
   design; byte-identity would mean it did not land. What is held byte-identical
   instead, and asserted in tests, is the shipped text against the v1 artifact.
5. The three descriptions are now single-line-per-paragraph and long. That is the
   measured lean form, deliberate, and pinned.
